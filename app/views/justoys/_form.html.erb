<%= form_for(justoy) do |f| %>
  <% if justoy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(justoy.errors.count, "error") %> prohibited this justoy from being saved:</h2>

      <ul>
      <% justoy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_area :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
